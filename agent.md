# Agent 工作流规则

本文件约束代码协作者(包括 AI Agent)在本仓库中的行为。

## 代码合并流程

- **每次合并代码前必须先发 Pull Request (PR)**
- PR 必须由用户审核并明确同意后,才能合并到目标分支
- 未经用户明确同意,禁止直接把代码合并到主分支(`1.21.1` 等受保护分支)
- 任何代码变更(新功能、修复、文档、构建配置调整等)都应通过 PR 提交

## 其他约定

- 仓库内已有 `ROADMAP.md` 与 `README.md`,新增功能需同步更新相关文档
- 构建由 GitHub Actions 完成,提交前无需进行本地构建
