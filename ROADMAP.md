# Roadmap / 已知限制

本文件记录 Phantom Staff Slot 的现状与后续计划，替代原 `task/task.txt`
（那份文件是项目早期的一份**无关需求稿**——要求做一个 Fabric 复制物品 ID 的模组，
与实际做出的 NeoForge 虚拟法杖槽位模组完全不同，已删除）。

## 当前状态

- 已完成：虚拟第 10 格、游戏内配置菜单（MaFgLib）、追踪红线、自动构建 CI。
- 已明确：仅兼容旧版 Create Aeronautics（2026-05-13 之前、服务端无 `validateWorthyness` 校验）。

## 后续计划

- [ ] 支持更多 Minecraft / NeoForge 版本（目前锁 1.21.1 / 21.1.x）。
- [ ] 追踪红线的颜色、长度、是否常显暴露到配置项。
- [ ] 增加数字键 `0` 直接选中第 10 格的便捷操作。
- [ ] 接入多语言（至少 `en_us.json`）。
- [ ] 在加入带 Aeronautics 的服务器时，做版本兼容检测并给出明确提示，
     避免在无法使用的新版服务器上误用（目前仅有启动期日志提醒）。
- [ ] 探索不依赖「服务端不校验」漏洞的替代实现路径，降低被踢风险。

## 不建议做

- 不计划在生存/公共服务器场景推广——本模组依赖已修复的漏洞，存在被封风险。
